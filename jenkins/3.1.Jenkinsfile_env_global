pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo "BUILD_NUMBER : ${BUILD_NUMBER}"
                echo "BUILD_NUMBER : ${env.BUILD_NUMBER}"
            }
        }
    }
}